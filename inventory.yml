all:

  children:

    etcd:

      hosts:

        etcd_1:

          ansible_host: "{{etcd_ip_1}}"

        etcd_2:

          ansible_host: "{{etcd_ip_2}}"

        etcd_3:

          ansible_host: "{{etcd_ip_3}}"

    pgbouncer:

      hosts:

        pgbouncer_1:

          ansible_host: "{{pgbouncer_ip_1}}"

        pgbouncer_2:

          ansible_host: "{{pgbouncer_ip_2}}"

    patroni:

      hosts:

        patroni_1:

          ansible_host: "{{patroni_ip_1}}"

        patroni_2:

          ansible_host: "{{patroni_ip_2}}"

        patroni_3:

          ansible_host: "{{patroni_ip_3}}"

        patroni_4:

          ansible_host: "{{patroni_ip_4}}"

    proxy:

      hosts:

        haproxy:

          ansible_host: "{{haproxy_ip}}"

  vars:

    ansible_connection: "ssh"

    ansible_port: "22"

    ansible_user: "user"

    ansible_ssh_private_key_file: "/home/user/.ssh/ssh-rsa.pub"
